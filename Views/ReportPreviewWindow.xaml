<Window x:Class="OnlineStoreApp.Views.ReportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Просмотр отчёта"
        Height="700" Width="1000"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#F1F5F9"  Icon="/Images/app.ico">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="54"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header toolbar -->
		<Border Grid.Row="0" Background="#1E293B" Padding="16,0">
			<Grid>
				<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
					<TextBlock Text="📄" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
					<TextBlock Text="Просмотр отчёта" Foreground="White"
                               FontSize="14" FontWeight="SemiBold" FontFamily="Segoe UI"
                               VerticalAlignment="Center"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
					<Button x:Name="RefreshButton" Content="🔄 Обновить"
                            Background="Transparent" Foreground="#94A3B8"
                            BorderThickness="0" FontFamily="Segoe UI" FontSize="12"
                            Cursor="Hand" Padding="10,0" Margin="0,0,8,0"/>
					<Button x:Name="OpenFolderButton" Content="📁 Папка"
                            Background="Transparent" Foreground="#94A3B8"
                            BorderThickness="0" FontFamily="Segoe UI" FontSize="12"
                            Cursor="Hand" Padding="10,0" Margin="0,0,8,0"/>
					<Button x:Name="PrintButton" Content="🖨 Печать"
                            Background="Transparent" Foreground="#94A3B8"
                            BorderThickness="0" FontFamily="Segoe UI" FontSize="12"
                            Cursor="Hand" Padding="10,0"/>
				</StackPanel>
			</Grid>
		</Border>

		<!-- WebView2 PDF viewer -->
		<wv2:WebView2 x:Name="Browser" Grid.Row="1"/>
	</Grid>
</Window>
